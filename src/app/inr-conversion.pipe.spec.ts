import { InrConversionPipe } from './inr-conversion.pipe';

describe('InrConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new InrConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
